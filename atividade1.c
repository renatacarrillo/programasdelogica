#include <stdio.h>
#include <stdlib.h>
#define MAX 20

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	
int i, v[MAX], p, valor;

for(i=0; i<20; i++)
{
printf("digite numero %d: ",i);
scanf("%d", &v[i]);
}

{
	printf("digite a posicao que deseja resgatar: ", p);
	scanf("%d", &p);
}

{
	v[valor]==p;
}
{
	printf("o valor referente a posicao e: ", valor);
	
}

return 0;
}
