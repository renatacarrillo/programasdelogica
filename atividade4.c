#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
int v1[20], i, soma, media, acumulador, p;

for(i=0; i<20; i++)
{
	printf("posicao %d: ",i);
	scanf("%d", &v1[i]);
}

{
	printf("insira o numero da posicao",p);
	scanf("%d", &p);
}

{
	acumulador = acumulador + v1[i];
}

{
	if(v1[i]==media);
	printf("a media dos 20 numeros foi de: ", media);
}
	return 0;
}
