#include <stdio.h>
#include <stdlib.h>

/* run this program using the console pauser or add your own getch, system("pause") or input loop */

int main(int argc, char *argv[]) {
	
int v1[20], v2[20], i, busca;

for (i=0; i<20; i++)
{
	printf("digite os numeros do v1 %d: ",i);
	scanf("%d", &v1[20]);
}
for (i=0; i<20; i++)
{
	printf("digite os numeros do v2 %d: ",i);
	scanf("%d", &v2[20]);
}
for (i=0; i<20; i++)
{
	if(vetor[i]==busca);
}
{
	printf("digite o numero que deseja buscar: \n");
	scanf("%d", &busca);
}



{
	printf("os indices onde foram encontrados sao: ", busca);
}


	return 0;
}
